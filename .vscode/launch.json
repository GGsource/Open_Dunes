{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Open Dunes Executable",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/OpenDunes.exe",
            "cwd": "${workspaceFolder}"
        }
    ]
}
