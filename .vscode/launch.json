{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Exe",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/OpenDunes.exe",
            "cwd": "${workspaceFolder}"
        }
    ]
}
